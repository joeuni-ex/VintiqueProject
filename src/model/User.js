export default class User {
  constructor(username, password, name, address, role, token, id) {
    this.username = username;
    this.password = password;
    this.name = name;
    this.address = address;
    this.role = role;
    this.token = token;
    this.id = id;
  }
}
